# Need Some Help

## Local setup

The app can be run locally by running:

```
dotnet watch
```

Github repository doesn't include appsettings.json file which has API keys needed for the app to run.

## Cryptocurrency

To use crypto features preferably install brave browser and set brave wallet there. Change solana network to devnet. The app is currently running on a remote network, but with fake money.
