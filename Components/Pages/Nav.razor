<div class="nav-bar">
    <NavLink href="/chats" class="@($"nav-item {(Section == "chats" ? "active" : "")}")">
        <RadzenIcon Icon="chat" class="nav-icon icon-filled" />
        <RadzenText Text="Chat" class="nav-text" TextStyle="TextStyle.Caption" />
    </NavLink>
    <NavLink href="/" class="@($"nav-item {(Section == "home" ? "active" : "")}")" Match="NavLinkMatch.All">
        <RadzenIcon Icon="favorite" class="nav-icon icon-filled" />
        <RadzenText Text="Help" class="nav-text" TextStyle="TextStyle.Caption" />
    </NavLink>
    <NavLink href="/add-request" class="@($"nav-item {(Section == "add-request" ? "active" : "")}")">
        <RadzenIcon Icon="add" class="nav-icon icon-filled" />
        <RadzenText Text="Request" class="nav-text" TextStyle="TextStyle.Caption" />
    </NavLink>
    <NavLink href="/profile" class="@($"nav-item {(Section == "profile" ? "active" : "")}")">
        <RadzenIcon Icon="person" class="nav-icon icon-filled" />
        <RadzenText Text="Profile" class="nav-text" TextStyle="TextStyle.Caption" />
    </NavLink>
</div>

@code {
    [Parameter]
    public string Section { get; set; } = "home";
}